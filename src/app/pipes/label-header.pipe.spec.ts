import { LabelHeaderPipe } from './label-header.pipe';

describe('LabelHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
